# Global defaults shared by every app. Per-app values in config/apps/<app>.env
# override anything set here.

# Platform used when an app config / CLI doesn't specify one.
DEFAULT_PLATFORM=android

# Opt out of Maestro's anonymous analytics for local/CI runs.
MAESTRO_CLI_NO_ANALYTICS=1
