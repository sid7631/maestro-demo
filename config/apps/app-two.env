# --- app-two -------------------------------------------------------------
# Example app configured for both platforms. Demonstrates that flows are
# shared/config-driven while binaries differ per app & platform.

APP_ID=com.example.apptwo
PLATFORM=android

# No Android binary here -> runner will launch-only (app assumed installed).
ANDROID_APP_BINARY=
IOS_APP_BINARY=binaries/app-two.app
